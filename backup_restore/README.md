## To run the solution
- Create a `.env` file, follow the sample
- Create a python venv, take the requirements.txt to install dependencies
- Do a `docker pull postgres:11` before running the code to get the solution in time
- Run the `runner.sh` script