# Solution to "Dockerized solutions"

### Setup

- ngrok installed and set up with user token
- podman installed/switch code to docker in `docker.cpp` if that is preferred
- cmake and ninja setup and working, clang++ compiler with cpp version >= 20
